services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      ARVANCLD_SESSION_PATH: /app/data/arvancld-session.json
    volumes:
      - ./data:/app/data
    restart: unless-stopped

